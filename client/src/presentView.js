import React from "react";

export default function DisplayPresents() {
	return (
		<div>
			<h1>I work</h1>
		</div>
	);
}
DisplayPresents();

//export default presentView;
/*useEffect(() => {
		fetch("/get-presents")
			.then(res => res.json())
			.then(json => {
				// upon success, update tasks
				console.log(json);
			})
			.catch(error => {
				// upon failure, show error message
			});
	}, []);*/
